// this holds everything in the topics page (i.e. division page)
import React, { Fragment } from 'react';
import Log_summary from './Log_summary';

export default function Dashboard_summary() {
    return (
        <Fragment>
            <Log_summary />
        </Fragment>
    )
}
